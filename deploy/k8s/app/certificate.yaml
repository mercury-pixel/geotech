apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: geodjango-cert
spec:
  secretName: geodjango-tls
  issuerRef:
    name: selfsigned-issuer
    kind: ClusterIssuer
  commonName: geodjango.local
  dnsNames:
    - geodjango.local